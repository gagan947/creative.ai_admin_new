import { Routes } from '@angular/router';
import { UsageCreditsComponent } from './components/usage-credits.component';

export default [{ path: '', component: UsageCreditsComponent }] satisfies Routes;
